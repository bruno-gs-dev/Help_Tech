/*
  Fill this file with your Firebase frontend config and deploy it.
  Copy values from the Firebase console (Project settings -> General).

  Example:
  window.FIREBASE_CONFIG = {
    apiKey: 'AIza...your_api_key...',
    authDomain: 'your-project.firebaseapp.com',
    projectId: 'your-project-id',
    // optional fields if you use them
    // storageBucket: 'your-project.appspot.com',
    // messagingSenderId: '...',
    // appId: '1:...:web:...'
  };

  IMPORTANT: keep this file out of public repos if it contains real keys.
  The Firebase web config is safe to include in frontend, but do not commit
  server credentials (service account private key) here.
*/

// TODO: replace the placeholder values below with your Firebase project's config
window.FIREBASE_CONFIG = {
  apiKey: 'REPLACE_WITH_YOUR_API_KEY',
  authDomain: 'REPLACE_WITH_YOUR_PROJECT.firebaseapp.com',
  projectId: 'REPLACE_WITH_YOUR_PROJECT_ID'
};
